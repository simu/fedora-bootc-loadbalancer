# TODOs

* Decision: Install netplan? or go directly through `nmcli` to customize network config?
* Figure out base set of firewall rules that we can bake into the image
  * should be fairly straightforward to define a firewalld policy set that covers the generic stuff that we want
* Build a version which includes bootc-loadbalancer-controller
